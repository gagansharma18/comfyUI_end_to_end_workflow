# Project Rules

## Platform Compatibility
- Whenever `run.bat` is modified or updated, ensure that `run.sh` is also updated to match the changes. This maintains cross-platform compatibility between Windows and macOS/Linux environments.
